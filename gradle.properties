# Faynx — Build Properties
# This file is intentionally minimal for a static web project.
# If you are building a companion Android app (e.g. with Capacitor or Cordova),
# add your Gradle config below.

org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true

# App version
VERSION_NAME=1.0.0
VERSION_CODE=1
